# website-admin

Some scripts (and possibly an admin website later on) to manage a website.

## Use

```bash
python -m wsa --conf bgc pull
python -m wsa --conf bgc build
python -m wsa --conf bgc serve
```
